import { css } from "linaria";

const Landing = () => {
  return (
    <h1
      className={css`
        color: var(--colors-tertiary);
      `}>
      Wow!
    </h1>
  );
};

export default Landing;
