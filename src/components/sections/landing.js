import { css } from "linaria";

const Landing = () => {
  return (
    <h1
      className={css`
        font-family: "mono", monospace;
      `}>
      Wow!
    </h1>
  );
};

export default Landing;
